# Optional: enables LLM-written SkyScout answers.
# Without this key, SkyScout still works with deterministic dashboard rules.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini

# Optional deployment overrides.
PORT=5173
HOST=127.0.0.1
